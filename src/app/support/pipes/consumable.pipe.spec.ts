import { ConsumablePipe } from './consumable.pipe';

describe('ConsumablePipe', () => {
  it('create an instance', () => {
    const pipe = new ConsumablePipe();
    expect(pipe).toBeTruthy();
  });
});
