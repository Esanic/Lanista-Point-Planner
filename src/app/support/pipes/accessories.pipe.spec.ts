import { AccessoriesPipe } from './accessories.pipe';

describe('AccessoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
