import { WeaponSkillsPipe } from './weapon-skills.pipe';

describe('WeaponSkillsPipe', () => {
  it('create an instance', () => {
    const pipe = new WeaponSkillsPipe();
    expect(pipe).toBeTruthy();
  });
});
